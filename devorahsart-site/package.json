{
  "name": "devorahsart-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler pages deploy . --project-name=devorahsart --commit-dirty=true"
  }
}
